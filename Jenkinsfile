pipeline {
  agent { label "Jenkins-Agent" }
  tools {
    jdk 'Java21'
    nodejs 'node16'    
  }
  environment{
    SCANNER_HOME=tool 'sonarqube-scanner'
  }
  stages {
    stage('') {
      steps {
        
      }
    }
  }
}
